# TaskEngine minimal example
# Usage: cd examples && taskengine trigger hello

tasks:
  hello:
    description: "Hello World example task"
    schedule: "*/5 * * * *"
    shell: bash
    steps:
      - name: greet
        command: "python hello.py"
